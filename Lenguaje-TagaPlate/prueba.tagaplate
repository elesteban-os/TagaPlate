-- Hola fjsdfjsd
@Principal (
    New @variable1, (Num, 5); 
    New @variable2, (Bool, True); 
);

Proc @proc1 (
    Values(@variable1, 51);
    Alter(@variable1, ADD, 5)
    AlterB(@variable2);
);

MoveRight;
MoveLeft;

Hammer(N);
Hammer(E);
Stop;